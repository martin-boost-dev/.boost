organizations:
  martin-boost-dev:
    repositories:
      test-terragoat:
        policy: "Block on related"
      labels-test:
        labels:
          label-terragoat:
            policy: "Block all"
      test-repo:
        policy: "notify-all-the-things"
