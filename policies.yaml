policies:
  - name: AWS Critical
    description: Prevent AWS related findings.
    location:
      registry_module: boostsecurityio/actions-by-label
    config_file: config/aws-critical-aws-config.yaml
