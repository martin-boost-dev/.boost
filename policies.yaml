policies:
  - name: My boost-category
    description: Prevent AWS related findings.
    location:
      custom: scripts/set-score.rego
  - name: My Boost-category
    description: Prevent AWS related findings.
    location:
      custom: scripts/set-score.rego
